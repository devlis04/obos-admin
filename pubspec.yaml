name: obos_admin
description: "Aplikasi web admin Obos (gaji)."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.13.0

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  flutter_dotenv: ^6.0.1
  supabase_flutter: ^2.17.2
  shared_preferences: ^2.5.5
  http: ^1.4.0
  intl: ^0.20.3
  file_picker: ^8.3.7
  excel: ^4.0.6
  web: ^1.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/supabase_config.txt
